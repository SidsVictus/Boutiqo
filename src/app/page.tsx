export default function Home() {
  return (
    <main style={{ padding: 40, fontFamily: "sans-serif" }}>
      <h1>Boutiqo — Phase 1 backend</h1>
      <p>
        This is Phase 1 scaffolding only. Product UI (Phase 2) is not built yet. The
        Supabase + Cloudflare R2 backend is exercised via the API routes under{" "}
        <code>/api/*</code> — see the repository README and{" "}
        <code>docs/phase1-report.md</code> for details.
      </p>
    </main>
  );
}
