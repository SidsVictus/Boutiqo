/** The exact icon set enumerated in CLAUDE_CODE_HANDOFF.md §7, mapped to their
 * lucide-react@0.454.0 glyph names. Import icons ONLY from here so the whole
 * app stays pinned to this exact set — never reach into lucide-react directly
 * from a screen. */
export {
  List,
  User,
  CalendarDays,
  Banknote,
  Settings,
  Store,
  Plus,
  Shield,
  Key,
  Search,
  Mic,
  Keyboard,
  Camera,
  ChevronRight,
  ChevronLeft,
  Check,
  Send,
  Scissors,
  Shirt,
  Package,
  Phone,
  Upload,
  LogOut,
  ImageIcon,
} from "lucide-react";
